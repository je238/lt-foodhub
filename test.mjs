import puppeteer from 'puppeteer'; (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.goto('http://127.0.0.1:8080/index.html'); const length = await page.evaluate(() => document.querySelectorAll('#login-err').length); console.log('Count: ', length); const text = await page.evaluate(() => document.querySelectorAll('#login-err')[0].outerHTML); console.log('first: ', text); const text2 = await page.evaluate(() => document.querySelectorAll('#login-err')[1].outerHTML); console.log('sec: ', text2); await browser.close(); })();
